FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONUNBUFFERED=1

EXPOSE 9090

CMD ["python", "-m", "uvicorn", "JOSAMICK.tools.c2_dashboard:app", "--host", "0.0.0.0", "--port", "9090"]
